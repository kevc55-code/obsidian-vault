# ByOwnerHub Network — Session 6 Notes (2026-07-01)

## What Was Done

### CF Pages Migration — Complete for 38 of 40 repos
All 38 byownerhub repos moved to Cloudflare Pages. DNS CNAMEs fixed for 21 domains that were pending. `nodejs_compat` compatibility flag set across projects. All redeployed successfully.

**fsbo-hub and solar-hub stay on Netlify** (not migrated — see standing notes in `network-status.md` Tier 1).

### investor-hub — Nav pages added, 404 root cause fixed
- `fix-and-flip/page.tsx`, `brrrr/page.tsx`, `buy-and-hold/page.tsx`, `financing/page.tsx` added to `src/app/` and pushed (these were drafted locally in Session 5 — now confirmed pushed).
- **Root cause of nav 404s found:** `netlify.toml` was misconfigured for server-render instead of static export. Fixed.

### boat-hub — 5 broken NVDC links fixed
Replaced `dcms.uscg.mil` → `dco.uscg.mil` NVDC eStorefront links (USCG reorganized their vessel documentation URLs). Commit `bba41bd`.

### land-hub — Riparian Rights section added
Added Riparian Rights section to the how-it-works guide. Commit `195cb75`.

### fsbo-hub — inline link + footer domain fix
- Contextual flatfee inline link added to `MLSComparison.tsx`.
- Footer domain fixed: `flatfee.byownerhub.com` was pointing to the wrong `flatfeemls.byownerhub.com` — corrected.
- Commit `c3a038a`.

### car-by-owner — 10 state form child pages added
Added state DMV/title form child pages: MV-912 (NY), REG 135 (CA), Form 130-U (TX), HSMV 82040 (FL), MV-4ST (PA), BMV 3774 (OH), VSD 703 (IL), T-7 (GA), MVR-1 (NC), SUT-1 (VA). Sitemap updated. Fixed a factual error on the Virginia page.

---

## Nothing Left Unpushed From This List

Unlike prior sessions, all items above were committed **and pushed** (commits `bba41bd`, `195cb75`, `c3a038a` + investor-hub nav push). Confirm no other local-only changes remain from this session.

---

## Open / Pending

- [ ] **GSC sitemaps for ~35 network sites — NOT yet submitted.** Highest-leverage remaining task; requires Kevin to do manually in Search Console (per-property verification).
- [ ] Plausible install — not done yet (see `open-items.md` Plausible section)
- [ ] `sameAs` social profiles in Organization schema — empty, waiting on real URLs from Kevin
- [ ] Stripe paywall ($99 document toolkit) — not yet built
- [ ] MH Village zip search broken — their side, follow up
- [ ] Author bylines + About page — not done
- [ ] Site bucketing decisions pending: fold closing/flatfee into fsbo; kill moto/RV/timeshare/boat/biz

---

## Carried Forward

See `network-status.md` for updated per-repo CF Pages status and `open-items.md` for the full network task list.
