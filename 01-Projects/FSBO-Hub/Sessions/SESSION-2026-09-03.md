---
type: session
project: FSBO-Hub
date: 2026-09-03
last-verified: 2026-09-04
---

# Session Log — 2026-09-03/04

> Vault sync covering the period since [[SESSION-2026-09-02]] (2 days). Headline: an **"under-contract / contract-stage" feature sprint** landed on both `fsbo-freemium-sandbox` (production) and `fsbo-staging` — 18 and 17 commits respectively, **all pushed / all in sync with origin**, dated 2026-09-03 (bulk) with an AI-route + price-tool fix tail on 2026-09-04. Same dev-first pattern as every recent sprint: staging re-built the set independently (same messages, different hashes). No other repo moved. The 2026-09-04 tail includes **the actual fix for the AI-route 400s** — a missing `anthropic-workspace-id` header — found only because the same sprint had just added "say which failure the AI routes hit" error surfacing (the identical lesson as the 08-26 Stripe key-ID incident).

---

## 1. "Under-contract / contract-stage" sprint — fsbo-freemium-sandbox (production), all pushed

Production's `master` moved `c2bb429` → `22b2643`, **18 commits, all pushed**, `master` in sync with `origin/master`. 15 commits dated 2026-09-03, 3 dated 2026-09-04.

**Contract-stage model + workspace deadline tracker:**
- `a2b7c23` **feat(data): add the contract-stage model**
- `7f1e6af` **feat(workspace): contract dates in the workspace model**
- `1992ca5` **feat(workspace): DeadlineTracker component**
- `b378865` **feat(workspace): bind the deadline tracker to the saved workspace** — the tracker persists into the same saved workspace the paid tier already uses.

**Under-contract pages (new per-state route):**
- `265968a` **feat(routes): add the under-contract page per state**
- `4698e84` **feat(links): link the under-contract pages from guides and metros** — wired in from state guides and metro pages so the new route isn't orphaned.

**State requirements matrix:**
- `2b40b21` **feat(routes): add the state requirements matrix**
- `1acfb87` **docs(state-requirements): record actual coverage, correcting the last commit** — same-day correction to an over-claimed coverage note.

**Partner co-branding + referral attribution:**
- `4744791` **feat(partners): carry `?ref=` through to the Stripe session and purchases row** — referral param now survives into checkout and is recorded on the `purchases` row.
- `1421805` **feat(partners): co-branded partner landing template**

**Copy / routing:**
- `49f5f9d` **feat(offer): attorney-state variant of the pitch**
- `ef105ee` **feat(routes): add the flat-fee handoff to the MLS comparison**

**Analytics:**
- `eaa34a6` **feat(analytics): add cookieless analytics and three funnel events**

**AI routes + the real 400 fix:**
- `1ef9ecb` **fix(ai): say which failure the AI routes hit, not "AI service error"** — replaces the generic catch string on the AI routes.
- `22b2643` (2026-09-04) **fix(ai): send anthropic-workspace-id, the actual cause of the 400** — the AI routes were returning 400; root cause was a missing `anthropic-workspace-id` header on the Anthropic API calls. Only visible *because* `1ef9ecb` had just stopped swallowing the cause. Same failure mode as the 08-26 Stripe key-ID incident — a generic catch string hiding a real auth/validation error.

**Price tool (2026-09-04):**
- `fcd1487` **fix(price-tool): stop requiring a metro the UI says is optional**
- `b058de3` **fix(price-tool): clear the error when moving between steps**

**Dev:**
- `9cc4b3d` **chore(dev): give production its own preview config name**

## 2. fsbo-staging — independently re-built the same sprint, all pushed

`fsbo-staging`'s `master` moved `f3ce1c8` → `6441f7e`, **17 commits, all pushed / in sync with `origin/master`**. Same messages as production, different hashes — the established dev-first flow. Includes the contract-stage model, DeadlineTracker, under-contract routes, state requirements matrix, partner co-branding + `?ref=`, attorney-state offer variant, flat-fee MLS handoff, cookieless analytics, the AI error-surfacing fix (`f599b35`) and the `anthropic-workspace-id` fix (`6441f7e`, 2026-09-04), and the price-tool fixes.

The only production commit staging lacks is `9cc4b3d` (production's own preview-config-name chore) — staging did its equivalent as `e84abb2` in the prior sprint. Staging is level with production.

## 3. Everything else — unchanged

- **fsbo-hub `main`** — no change, still `e659fd1`, in sync with origin, working tree clean. `freemium-wip` still 11 commits ahead of origin, unchanged since 08-05 (now ~5 weeks / eight vault-check sessions; deploy-readiness unverified since 07-12; still likely superseded by production's real Stripe build — [[open-items]] item 8). Several stale `claude/*` worktree branches sit behind origin — cosmetic, pre-existing.
- **car-by-owner** — no change since `ba17a8d` (2026-09-02); `main` in sync.
- **fsbo-freemium-sandbox** — `deploy.bat` and `deploy_freemium_branch.ps1` still untracked; the plaintext GitHub PAT in the latter still unrotated (**~43 days** since flagged 07-23, file mtime unchanged).
- **buyer-hub** — `tools/network-audit/{report.md,results.json}` still an uncommitted modification holding the **2026-09-01** weekly-audit run — no new audit since, unchanged from the last vault update.
- **Local-clone pointer staleness (not unpushed work):** funeral-hub `master` behind 7, investor-hub `master` behind 6, landlord-hub `claude/fervent-almeida-217d54` behind 20 — origin has everything; the local clone just isn't fast-forwarded.
- **Mirror-branch drift** — ~21 repos per the 09-01 audit, persisted (see [[open-items]] 09-01 line). Cosmetic per [[network-audit-automation]] but a batch sync is overdue.
- No other repo has new commits or uncommitted changes since 09-02.

## 4. Follow-ups worth a human eye

- **Live-verify the AI routes** on fsbo.byownerhub.com — `22b2643` claims the `anthropic-workspace-id` header was the 400 cause; confirm the listing-description and price-estimate tools actually work in production now.
- **Under-contract routes + state requirements matrix** are new indexable pages — check they're in `sitemap.ts` output and not orphaned (the `4698e84` link commit suggests they were wired, but verify the sitemap picked them up).
- **`?ref=` attribution** now writes to the `purchases` row — worth confirming the column exists in the production Supabase schema and the webhook handler tolerates its absence for non-referral purchases.
