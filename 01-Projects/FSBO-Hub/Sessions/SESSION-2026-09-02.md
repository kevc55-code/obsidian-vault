---
type: session
project: FSBO-Hub
date: 2026-09-02
last-verified: 2026-09-03
---

# Session Log — 2026-09-02

> Vault sync covering the period since [[SESSION-2026-08-25]] (8 days). Headline: a **"positioning + commission" copy/data sprint on 2026-09-02** landed on all three fsbo repos — production (`fsbo-freemium-sandbox`) got 6 commits, `fsbo-staging` independently re-built the same set (dev-first flow), and **fsbo-hub `main`'s long-uncommitted `Hero.tsx` edit finally got committed and pushed** (`e659fd1`), clearing an item carried since 08-21. car-by-owner shipped a page-level FTC-disclosure commit. The weekly link audit (09-01) and the affiliate-decline digest (09-02) are already logged in [[open-items]] by other runs.

---

## 1. "Positioning + commission" sprint — fsbo-freemium-sandbox (production), all pushed

Production's `master` moved `f955716` → `c2bb429`, 6 commits, all 2026-09-02, all pushed:

- `2feef13` **fix(stripe): log and surface the failure class on checkout errors** — follow-up to the 2026-08-26 key-ID incident (the `catch` in `src/app/api/stripe/checkout/route.ts` returned a generic string, which is why a live-mode 401 hid for three weeks). Now logs and surfaces the Stripe error class. +21/-2 in one file.
- `956f00d` **feat(copy): add positioning module** — new homepage positioning section.
- `27438b5` **feat(copy): state the free/paid split on the homepage hero** — hero now says outright what's free vs. what's behind the $99 paywall.
- `5e0fe84` **feat(data): derive commission figures from median price in one place** — new `src/lib/commission.ts` as the single basis.
- `4af6391` **feat(metro): show both commission figures instead of one ambiguous number** — metro page now shows both figures rather than one number readers couldn't interpret.
- `c2bb429` **fix(commission): one basis everywhere, and retire the drifted field** — routes the suburb page, `opengraph-image.tsx`, `SavingsCalculator`, and `SchemaMarkup` all through `commission.ts`; removes a drifted field from `src/types/index.ts`. 6 files, +57/-24.

## 2. fsbo-staging — independently re-built the same sprint, plus committed its pending edit

`fsbo-staging`'s `master` moved to `f3ce1c8`, 7 commits, all 2026-09-02, all pushed / in sync with `origin/master`:

- `1bf2a10` **seo: match production's crawler policy and drop the dead SearchAction** — this is the previously **uncommitted** local edit flagged in [[SESSION-2026-08-25]] §3, now committed. Staging's independent re-implementation of production's `1cd6b5c`.
- `c75b5c1` / `ec2d53a` — positioning module + free/paid hero split (mirror of production's `956f00d` / `27438b5`).
- `e84abb2` **chore(dev): give staging its own preview config name** — `.claude/launch.json` name so staging and production don't collide in the preview tooling.
- `664cec2` / `7c8e716` / `f3ce1c8` — the commission-in-one-place / show-both-figures / one-basis set (mirror of production's `5e0fe84` / `4af6391` / `c2bb429`).

Same dev-first pattern as prior sessions — different hashes, same messages. Staging is now essentially caught up to production; the only prod commit it lacks is the prod-specific Stripe error-logging fix (`2feef13`).

## 3. fsbo-hub `main` — the Hero.tsx edit finally landed

`e659fd1` **refactor(hero): drop the metro hero CTAs and tighten the spacing** (2026-09-02) — this is the uncommitted `src/components/Hero.tsx` edit that had been sitting in the working tree since **2026-08-21** (flagged across five vault-check sessions). Now committed **and pushed**; `main` is in sync with `origin/main` at `e659fd1` and the working tree is clean. Closes the item.

`freemium-wip` is unchanged — still 11 commits ahead of `origin/freemium-wip`, unchanged since 08-05 (now 4 weeks / seven vault-check sessions). Deploy-readiness still not re-verified since 07-12. Still likely superseded by production's real Stripe build — see [[open-items]] item 8.

## 4. car-by-owner — FTC disclosure

`ba17a8d` **affiliate: page-level FTC disclosure above CTAs; tidy Carfax note** (2026-09-02) — pushed, `main` in sync with origin.

## 5. Everything else — unchanged

- **fsbo-freemium-sandbox** — `deploy.bat` and `deploy_freemium_branch.ps1` still untracked; the plaintext GitHub PAT in the latter still unrotated (**~41 days** since flagged 07-23). Also a staged-but-uncommitted `.claude/launch.json` rename (`"fsbo-hub"` → `"fsbo-production"`) — trivial, matches staging's `e84abb2`.
- **buyer-hub** — `tools/network-audit/{report.md,results.json}` still an uncommitted modification (now the **2026-09-01** weekly-audit run). Same open question as before: commit each run or leave as scratch.
- **Mirror-branch drift** — ~21 repos per the 09-01 audit, persisted across two weekly runs (see [[open-items]] 09-01 line). Cosmetic per [[network-audit-automation]] but a batch sync is overdue.
- No other repo has new commits or uncommitted changes since 08-25.
