# Session Log — 2026-07-07 → 2026-07-12 (sessions 8–10)

> Catch-up log covering everything since the 2026-07-04 link-audit session.
> Detail lives in repo docs: `buyer-hub/byownerhub-audit-2026-07-07.md`, `buyer-hub/PRIVACY-REQUEST-RUNBOOK.md`.

---

## 1. Legal entity formed — **Byownerhub.com LLC** (New Mexico)

- Registered agent / mailing address: **1209 Mountain Road Pl NE, Ste N, Albuquerque, NM 87110** (Northwest Registered Agent).
- This address is the CAN-SPAM physical address in fsbo email footers and appears on privacy/terms pages.
- **2026-07-12: network-wide footer sweep** — every site footer now reads `© {year} Byownerhub.com LLC` and links to `https://byownerhub.com` ("Part of the ByOwnerHub network"). ~40 repos, 101 files. fsbo's old "FSBO Hub LLC" (nonexistent entity) corrected everywhere incl. 57 state-guide inline footers + 4 Spanish pages. landlord/probate footers had no © line at all — added.
- Stripe account (for freemium) should be opened under this LLC.

## 2. Accessibility — network-wide WCAG 2.1 AA pass

- ~1,300 axe violations → 0 across the network. **36/36 CF sites verified axe-clean LIVE**; fsbo (Netlify) also clean.
- Method: local build → serve `out/` → axe (puppeteer) → fix → re-verify. Contrast fixes via darkened brand tokens + `-bright` companions on dark backgrounds; `grayscale` instead of wrapper opacity for gated content.

## 3. Email compliance (CAN-SPAM) — fsbo pipeline rebuilt & live-verified

- Unsubscribe was cosmetic (no token ever issued). Now: token on every signup, real `/api/unsubscribe?token=` link, `List-Unsubscribe` + RFC 8058 one-click POST, sets `unsubscribed_at` + `is_active=false`, suppresses `leads` too. Verified end-to-end against live DB.
- Consent microcopy at every collection point; LLC address in email footer.
- Five sibling façade forms (biz NDA, estate/str/eviction checklists, moto bill-of-sale) now POST to `fsbo.byownerhub.com/api/subscribe` (CORS-enabled) with per-site `source` tags; each got a `/privacy` page naming the LLC.
- Deletion/access runbook: `buyer-hub/PRIVACY-REQUEST-RUNBOOK.md` (45-day SLA, Supabase SQL snippets).

## 4. Netlify (fsbo, rent, car)

- Account ran out of credits (64 deploys ≈ 960 credits) → deploys disabled → **re-upped 2026-07-11**, everything pushed and live-verified.
- Census zip lookup fixed twice: (a) `Netlify-Vary: query=zip` — CDN cache key ignores query strings by default so every zip served the first zip's data; (b) clamp ACS `-666666666` sentinels to 0. Requires `CENSUS_API_KEY` env var (set). `ANTHROPIC_API_KEY` set (Functions scope).
- Still unset: `RESEND_API_KEY` (welcome emails silently skipped until then).

## 5. Freemium ($99 toolkit) — branch `freemium-wip`, DEPLOY-READY, not deployed

- Merged main in (af732a6); fixed PaywallGate (dead sage/forest tokens, dialog a11y, Escape-close, softened "attorney-vetted" claim), checkout/success (buyer capture → `toolkit-purchase` source), terms got "Purchases & Refunds" (30-day refund).
- Verified: full build, axe-clean incl. open modal + checkout pages; Stripe routes fail clean without key. Head `695c8df`.
- **Launch = merge freemium-wip→main + set `STRIPE_SECRET_KEY` on Netlify + deploy.** Entitlement is localStorage-only; add webhook + purchases table if it sells.

## 6. Alias-domain 301s (duplicate-content fix)

- CF Pages `_redirects` host rules silently no-op without domain attachment → used **Pages Function `functions/_middleware.js`** host-check 301 instead. Live: `flatfee→flatfeemls`, `newbuild→new-build`, `frbo→rent` (frbo after Kevin added DNS + custom domain).

## 7. new-build state pages

- 50 placeholder state pages → real content (`src/lib/state-content.ts`: metros/market/building/costs per state + 7 universal steps + FAQs with FAQPage JSON-LD). noindex removed, canonical + sitemap restored.

## 8. GSC

- Analyzed 2026-07-11 performance export. Sitemap API submission for all 38 properties failed 403 — the service account was never added as a user. Fix: add service-account client_email as Full user on `sc-domain:byownerhub.com`, or paste sitemap URLs manually.

---

## Open items → see [[open-items]] (updated 2026-07-12)
