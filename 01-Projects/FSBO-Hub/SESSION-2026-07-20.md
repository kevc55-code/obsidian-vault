# Session Log — 2026-07-20

> Vault sync covering the period since the 2026-07-14 next.js security-bump sweep. Hosting/deploy status is unchanged from [[SESSION-2026-07-12]] — this session's findings are all in git state: new code committed but unpushed, plus one significant discovery of uncommitted parallel work.

---

## 1. next.js `^14.2.35` security sweep (already recorded 2026-07-14, confirmed still closed)

26 repos bumped off vulnerable 14.x, committed + pushed to current branch and mirror where both exist. No new action — carried forward from [[open-items]] item 0b for continuity.

## 2. Orphan-sitemap fixes for GSC's "9 missing pages" item — code done, not pushed

Same-day commits (~07:46–07:47) across three repos, closing the open item:
- **fsbo-hub** `588daec` — added `/unsubscribed`, `/tools/listing-description`, `/tools/price-my-home` to sitemap.
- **condo-hub** `e4c4269` — added disclaimer/privacy/terms to sitemap.
- **investor-hub** `7bfaa09` — added disclaimer/privacy/terms to sitemap.

None of these three commits have been pushed to `origin`. See [[unpushed-changes]].

## 3. fsbo-hub GSC "Crawled - not indexed" fixes — code done, not pushed

Two more same-day fsbo-hub commits, also unpushed:
- `bd6054d` — metro blog index pages always noindexed (still followed, so link equity reaches real posts). These are navigational shells GSC was flagging as thin content.
- `a953599` — **suburb pages enriched with real Census data**, closing the root cause tracked in [[fsbo-suburb-thin-content]]. The old intro generator only had 5 fields and 4 price-ratio buckets → near-identical prose across hundreds of suburbs. Fix reverses the previously-unused `ZIP_TO_SUBURB` map, extends it with 416 more real ZIP matches from GeoNames, and appends real Census ACS facts (median household income, homeownership rate) per suburb ZIP where available. 688/779 suburbs (88%) now get differentiated content; remaining 12% fall back to old copy.

## 4. ⚠️ Discovery: parallel freemium/Stripe build on fsbo-hub `main`, uncommitted

While checking fsbo-hub's working tree, found substantial **uncommitted** changes building the $99 toolkit paywall a second time, independently of the already-deploy-ready `freemium-wip` branch (see [[open-items]] item 8):

- New untracked files: `src/app/api/stripe/checkout/route.ts`, `src/app/api/stripe/verify/route.ts`, `src/app/checkout/success/page.tsx`, `src/app/checkout/cancel/page.tsx`
- Modified: `src/lib/email.ts` (new `sendToolkitEmail` function, different post-purchase email template/copy than freemium-wip's), `src/components/ChecklistAndForms.tsx` (different gating copy — "checklist + 2 forms free, rest for $99" vs. freemium-wip's full-checklist-free framing)
- Implementation differs from freemium-wip's version too: different Stripe SDK `apiVersion` (`2023-10-16` vs. freemium-wip's `2024-04-10`), and this WIP version also collects email in the Stripe checkout session for lead capture.

This is not committed anywhere — pure working-tree state, at risk of being lost on any `git clean`/`checkout`. Two competing implementations of the same feature now exist. Flagged in [[open-items]] as a Kevin-only decision: pick one approach, then either fold the `main` WIP into `freemium-wip` or discard it. **Do not push `main` as-is** — fsbo-hub auto-deploys `main` via Netlify, and this would ship an unreviewed, half-built paywall variant to production alongside the 3 legitimate unpushed fixes from §2–3 above.

## 5. Other repos checked

All ~40 repos in `C:\Users\kevc_\Documents\GitHub` (excluding `equiconnected`) were checked for commits since 2026-07-14 and working-tree status. Nothing else found beyond the next.js bump commits (already recorded) and the items above. `buyer-hub`'s `tools/network-audit/{baseline.json,report.md,results.json}` remain untracked local run artifacts, unchanged since 2026-07-13 — not flagged as an issue.

---

## Open items → see [[open-items]] (updated 2026-07-20)
